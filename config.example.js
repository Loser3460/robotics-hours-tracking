// Copy this file to config.js and fill in your deployed Apps Script Web App URL.
// config.js is gitignored — never commit the real URL.
//
// Deploy: Apps Script editor -> Deploy -> New deployment -> Web app
//   Execute as:      Me
//   Who has access:  Anyone
// Copy the /exec URL it gives you into API_URL below.

export const API_URL = 'https://script.google.com/macros/s/PASTE_DEPLOYMENT_ID_HERE/exec';
